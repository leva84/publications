<p style="color: green"><%= notice %></p>

<h1>Publication cards</h1>

<div id="publication_cards">
  <% @publication_cards.each do |publication_card| %>
    <%= render publication_card %>
    <p>
      <%= link_to "Show this publication card", publication_card %>
    </p>
  <% end %>
</div>

<%= link_to "New publication card", new_publication_card_path %>
